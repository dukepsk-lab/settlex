# Runtime dependencies for settlex (mirror of pyproject [project.dependencies])
pandas>=2.0
numpy>=1.26,<2.0
scikit-learn>=1.3
xgboost>=2.0
tensorflow>=2.16
PyPortfolioOpt>=1.5
python-dotenv>=1.0
pyarrow>=12.0
requests>=2.28
yfinance>=0.2.40
settrade-v2>=2.2.1

# Optional: enables ADF-based fractional-differencing order search (features/fracdiff.py)
# statsmodels>=0.14

# Optional: LLM providers for the `briefing` command (pip install -e ".[llm]")
# anthropic>=0.40
# google-genai>=0.3
# openai>=1.40

# Dev/testing
pytest>=7.0
